<% for post in @posts do %>
	$('.posts').append("<li class='post <%='unpublished' unless post.published?%>'><%= j render(post) %></li>");
<% end %>

<% if @posts.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@posts) %>');
<% else %>
$('.pagination').remove();
<% end %>